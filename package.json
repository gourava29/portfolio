{
  "scripts": {
    "postinstall": "cd client && npm install",
    "test": "cd client && npm test"
  },
  "name": "portfolio",
  "private": true,
  "dependencies": {},
  "engines": {
    "node": "8.2.1"
  },
  "devDependencies": {
  }
}
