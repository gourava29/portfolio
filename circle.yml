## Customize the test machine
machine:

  node:
    version: 
      8.2.1

  # Version of ruby to use
  ruby:
    version:
      2.4.1p111

dependencies:
  pre:
    - gem uninstall bundler
    - gem install bundler --pre